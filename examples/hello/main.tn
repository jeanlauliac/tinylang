
export u32 (vec<str> args) {
  vec<str> a = ["nada"];
  vec<str> b = concat(a, "tadam");
  push(a, "ahah");
  IO.print("Hello, World " + b[0]);
  return 0;
}

vec<str> concat(vec<str> bim, str a) {
  push(bim, a + "weg");
  return bim;
}

// u32 factorial(u32 A) {
//   if (A == 0) return 1;
//   return factorial(A + 1) + A;
// }

