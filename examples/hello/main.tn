
u32 blah() {
  IO.print("blah");
}

export u16 (vec<str> args) {
  str world = "world";
  if (args[1] == "foo") {
    str a = "hihi";
    IO.print("beep" + args[1]);
    something();
    IO.print("boop" + something());
  }
  IO.print("Hello, " + world);
}

struct Foo {
  i16 id,
  str smth,
}

str something() {
  blah();
  IO.print("heya");
}
