/**
 * Meant to compile as a Node.js script.
 */
export u16 (vec<str> args) {
  str world = "world";
  if (args[1] == "foo") {
    IO.print("beep");
    IO.print("boop");
  }
  IO.print("Hello, " + world);
}
