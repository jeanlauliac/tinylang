
str blah(str smth) {
  return "blah" + smth;
}

export u32 (vec<str> args) {
  str world = "world";
  if (args[1] == "foo") {
    str a = "hihi";
    IO.print("beep" + args[1]);
    something("BOOOP");
    IO.print("boop");
  }
  IO.print("Hello, " + world);
  return 0;
}

struct Foo {
  i16 id,
  str smth,
}

str something(str smth) {
  str a = blah(smth);
  IO.print("heya" + a);
  return "wef";
}
