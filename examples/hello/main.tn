
export u32 (vec<str> args) {
  vec<str> a = ["nada"];
  foo(a, "tadam");
  push(a, "ahah");
  IO.print("Hello, World " + a[0]);
  return 0;
}

void foo(vec<str> bim, str a) {
  push(bim, a + "weg");
}

/*
// void foo(vec<str> bim) {
//   push(bim, "weg");
// }

// void bar(vec<str> bim) {
//   vec<str> smth = ["ahah"];
//   foo(smth);
//   push(bim, "weg");
// }


*/
